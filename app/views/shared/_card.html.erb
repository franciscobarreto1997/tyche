<% @items.each do |item| %>
  <%= link_to item_path(item) do %>
    <div class="card">
      <div class="picture">

      </div>
      <div class="info">
        <h4><%= item.title %></h4>
      </div>
  </div>
  <% end %>
<% end %>
